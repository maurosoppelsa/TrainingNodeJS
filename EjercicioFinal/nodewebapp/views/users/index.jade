h1.
    #{title}
    ul
    - each blob, i in blobs
    li
        = user.name
        = user.badge
        = user.dob
        = user.isloved
        = user._id
        form(action='/users/#{user._id}/edit',method='post',enctype='application/x-www-form-urlencoded')
        input(type='hidden',value='DELETE',name='_method')
        button(type='submit').
            Delete
    p
        a(href='/blobs/#{user._id}/edit') Edit
    p
    a(href='/user/#{user._id}') Show